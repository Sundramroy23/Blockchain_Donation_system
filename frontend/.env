# .env  (place this in your React project root, next to package.json)
# Frontend dev server default port
PORT=3000

# Base backend origin only (do not append /api/users).
REACT_APP_API_URL=http://localhost:5000
